@model IEnumerable<hunterview.Models.JobCoordination>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Adddate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Approved)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Adddate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Approved)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.JobCoordinationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.JobCoordinationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.JobCoordinationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
